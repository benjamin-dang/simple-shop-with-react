import { AppBar, Toolbar, Typography, Box, Grid } from "@mui/material";

const Header = () => {
    return (
        <AppBar position="fixed">
            <Toolbar>

            </Toolbar>
        </AppBar>
    )
}

export { Header };